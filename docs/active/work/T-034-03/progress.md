# T-034-03 Progress: agent-test-targeting

## Completed

1. ✅ Added `_test-file FILE` private recipe to `.just/system.just`
2. ✅ Added `test-file FILE` public recipe to `justfile`
3. ✅ Updated RDSPI workflow implement phase with explicit domain-targeting guidance
4. ✅ Verified `just test-stale` works from agent shell
5. ✅ Verified `just test-file` works from agent shell

## Already done (prior work)

- `just test-stale` recipe — already existed
- `_llm` recipe `--stale` mention — already present (line 270)
- RDSPI `mix test --stale` mention — already present, now strengthened

## Deviations

None. All AC items addressed.
