# T-012-01 Progress: Tenant Resolver Plug

## Completed Steps

### Step 1: Migration ✓
- Created `priv/repo/migrations/20260309022659_add_domain_to_companies.exs`
- Adds nullable `domain` text column + unique index to companies table
- Generated by `mix ash_postgres.generate_migrations`

### Step 2: Company resource ✓
- Added `domain` attribute (nullable string, public)
- Added `identity :unique_domain, [:domain]`
- Updated `:create_company` and `:update_company` to accept `:domain`

### Step 3: Config ✓
- Added `config :haul, :base_domain, "localhost"` to config.exs
- Added `BASE_DOMAIN` env var support in runtime.exs
- Added `config :haul, :base_domain, "haulpage.test"` in test.exs

### Step 4: TenantResolver plug ✓
- Created `lib/haul_web/plugs/tenant_resolver.ex`
- Resolution chain: custom domain → subdomain → fallback
- Uses `Ash.Query.filter` + `Ash.read_one` for Company lookups
- Assigns `current_tenant` (Company struct or nil) and `tenant` (schema string)

### Step 5: Router integration ✓
- Added `plug HaulWeb.Plugs.TenantResolver` to `:browser` pipeline
- Created `:api_with_tenant` pipeline for places API
- Webhooks and healthz remain outside tenant resolution

### Step 6: Tests ✓
- Created `test/haul_web/plugs/tenant_resolver_test.exs` with 12 tests
- Covers: subdomain resolution, custom domain resolution, fallback, extract_subdomain

### Step 7: Full suite verification ✓
- 224 tests, 0 failures (up from 212 baseline)

## Deviations from Plan
- Used `Ash.Query.filter` + `Ash.read_one` instead of `Ash.read_one(filter:)` — the latter isn't a valid Ash API
