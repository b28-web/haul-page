# T-019-03 Progress: Conversation Persistence

## Completed Steps

### Step 1: AI Ash Domain + Conversation Resource ✅
- Created `lib/haul/ai/domain.ex` — Ash domain `Haul.AI.Domain`
- Created `lib/haul/ai/conversation.ex` — full resource with all actions
- Created `lib/haul/ai/changes/append_message.ex` — change module for add_message
- Added `Haul.AI.Domain` to `ash_domains` in config
- Generated and ran migration `20260309050724_create_conversations.exs`
- Table: `conversations` with unique session_id index, status+inserted_at index, company_id index

### Step 2: Conversation Resource Tests ✅
- Created `test/haul/ai/conversation_test.exs` — 15 tests
- Covers: create, duplicate rejection, add_message, message ordering, atom keys, save_profile, link_to_company, mark_abandoned, stale_active queries, old_abandoned queries, destroy

### Step 3: EnsureChatSession Plug ✅
- Created `lib/haul_web/plugs/ensure_chat_session.ex`
- Added to `:browser` pipeline in router

### Step 4: ChatLive Persistence ✅
- Modified `mount/3` to load/create conversation from session cookie
- Modified `send_user_message/2` to persist user messages to DB
- Modified `handle_info({:ai_done})` to persist assistant messages to DB
- Modified `handle_info({:extraction_result})` to save extracted profile to conversation
- Added `deep_to_map/1` helper to convert nested structs for JSON serialization

### Step 5: CleanupConversations Worker ✅
- Created `lib/haul/workers/cleanup_conversations.ex` — Oban cron worker
- Added to cron config (3am daily)
- Created `test/haul/workers/cleanup_conversations_test.exs` — 4 tests

### Step 6: Integration Testing ✅
- All 602 tests passing, 0 failures
- Existing chat_live tests pass without modification (17 tests)
- New conversation tests: 15 tests
- New cleanup worker tests: 4 tests

## Deviations from Plan

- Removed spurious tenant migration auto-generated by `mix ash.codegen` (was adding columns from other tickets' resources to tenant tables)
- Added `deep_to_map/1` to handle `OperatorProfile.ServiceOffering` struct serialization — `Map.from_struct/1` is shallow
- Resource snapshots for other Ash resources were regenerated by codegen — these are harmless and track current resource state
